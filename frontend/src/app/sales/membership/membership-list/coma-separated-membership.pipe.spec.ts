import { ComaSeparatedMembershipPipe } from './coma-separated-membership.pipe';

describe('ComaSeparatedMembershipPipe', () => {
  it('create an instance', () => {
    const pipe = new ComaSeparatedMembershipPipe();
    expect(pipe).toBeTruthy();
  });
});
