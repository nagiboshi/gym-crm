import { DelimeterSeparatedValuePipe } from './delimeter-separated-value.pipe';

describe('DelimeterSeparatedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DelimeterSeparatedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
